# Processed BED files

This directory will contain standardized BED files generated from the original capture target region files stored in:

`data/raw_beds/`

Each processed BED file must be traceable back to its corresponding raw file.  
The relationship between raw and processed files will be recorded in:

`metadata/capture_probes_metadata.tsv`

