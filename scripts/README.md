# Scripts

This directory contains scripts used to reproduce the downloading, preparation, and downstream analysis of capture probe target region files.

The main metadata source used by these scripts is:

`metadata/capture_probes_metadata.tsv`
